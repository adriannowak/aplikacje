---
title: JS
layout: post
---


### Lekcja 1
```javascript
console.log("Hello - plik zewnętrzny");

let imie = "Adrian";
let wiek = 30.5;
let zawod = ["Nauczyciel", "Programista"];
let czy_lubi_informatyke = true;

console.log("Imie = ",imie, 
	"wiek = ", wiek, 
	"zawod = ", zawod, 
	"czy_lubi_informatyke", czy_lubi_informatyke);

imie = "Janek";
wiek = 20;
zawod = "Uczen";

console.log("Imie = ",imie, 
	"wiek = ", wiek, 
	"zawod = ", zawod, 
	"czy_lubi_informatyke", czy_lubi_informatyke);

// tutaj deklarujemy obiekt
let butelka = {
	objetosc : 1.25,
	zawartosc : ["woda","E250", "konserwanty"],
	wymiary: [100,200],
	napijsie : function() {
		console.log("PIJE!");
	}
};

/**
    tutaj wyświetlamy informacje o obiekcie
**/

console.log(butelka);
console.log(butelka.objetosc);
butelka.napijsie();

butelka.objetosc=2;
console.log(butelka);

// okno informacyjne
window.alert("Hello world!");

// okno decyzyjne
let odpowiedz = window.confirm("Czy jesteś teraz na lekcji?");
console.log(odpowiedz);

// okno tekstowe
imie = window.prompt("Wprowadz swoje imie");
console.log(imie);
```
#### Zadanie

Zadanie:
    Przygotuj obiekt klient z polami:
     - imie
     - nazwisko
     - wiek
     - zgoda_marketingowa

   Uzupełnij zmienne przy użyciu window.prompt, window.confirm oraz 
   uzupełnione wartości wyświetl przy uzyciu window.alert oraz console.log


## Lekcja 2

```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body id="body">
      <!-- dopiero po załadowaniu elementu body
      możemy modyfikować zawartość strony WWW
      -->
      <script>
        let body = document.getElementById("body");
        //console.log(body);

        // bblock - text
        let text = document.createTextNode("Hello world!");
        body.appendChild(text);

        // bblock - dowolny znacznik HTML
        let paragraph = document.createElement("p");
        let paragraphContent = document.createTextNode("Przykladowy akapit");

        // kolejnosc dodawania jest bez znaczenia
        body.appendChild(paragraph);
        paragraph.appendChild(paragraphContent);

        // **************************************
        // Mozna łatwiej!

        let paragraph2 = document.createElement("p");
        paragraph2.textContent = "Tresc akapitu";
        body.append(paragraph2);

        // **************************************
        // OK, a gdybym chcial wstawic tekst pomiedzy dwa
        // akapity ??
        // - insertBefore
        // - insertAfter
        // Kazda z tych funkcji przyjmuje 2 parametry
        // - NOWY element
        // - ISTNIEJACY element
        // UWAGA! Operacje wykonujemy na RODZICU!

        let paragraph3 = document.createElement("p");
        paragraph3.textContent = "Ostatni element";
        //body.append(paragraph3;
        body.insertBefore(paragraph3, paragraph);


        //**************************************
        // Usuniecie elementu
        // removeElement
        // Uwaga: stosujemy na elemencie nadrzednym
        body.removeChild(text);


        // *************************************
        // Podmiana elementu
        // replaceChild(new,old)
        // Uwaga: stosujemy na elemencie nadrzednym
        let paragraph4 = document.createTextNode("Paragraf 4");
        body.replaceChild(paragraph4, paragraph3);
        
      </script>
    </body>
</html>
```


```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body id="body">
     <ul id="lista">
       <li id="remove">Ten element jest do usunięcia</li>
       <li id="replace">Ten element jest do modyfikacji</li>
       <li id="move">Ten element jest do przesunięcia na pozycje 1</li>
      <li>... wstaw</li>
     </ul>
    <script>
      // Punkt 1 - OK
      let list = document.getElementById("lista");
      let remove = document.getElementById("remove");
      list.removeChild(remove);

      // Punkt 2 - OK
      let newValue = document.createElement("li");
      newValue.textContent = "Nowa zawartosc";
      let replace = document.getElementById("replace");

      list.replaceChild(newValue, replace);

      // Punkt 2 - alternatywa
      newValue.textContent = "Nowa zawartosc2";

      // Punkt 3 - OK
      let move = document.getElementById("move");
      list.removeChild(move);
      list.insertBefore(move, newValue);

      
</script>
    </body>
</html>
```


### Zadanie

```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body id="body">
      <!-- <h1>Lista uczestników szkolenia:</h1>
      <ul>
        <li>Piotr Kowalski</li>
        <li>Adam Nowak</li>
        <li>Monika Kowalska</li>
      </ul> -->
    <script>
      let body = document.getElementById("body");

      let h1 = document.createElement("p");
      h1.textContent = "Lista uczestników szkolenia:";

      let list = document.createElement("ol");
      
      let li_1 = document.createElement("li");
      let li_2 = document.createElement("li");
      let li_3 = document.createElement("li");
      
      li_1.textContent = "Piotr Kowalski";
      li_2.textContent = "Adam Nowak";
      li_3.textContent = "Monika Kowalska";

      list.appendChild(li_1);
      list.appendChild(li_2);
      list.appendChild(li_3);

      body.appendChild(h1);
      body.appendChild(list);

      li_1.style.color = "red";
      li_1.style.backgroundColor = "yellow";
      li_1.style.fontSize = "30px";

      
    </script>
    </body>
</html>
```


### Zadanie 2
```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body id="body">
    <button onclick="wstawObraz()">Dodaj obraz</button>
    <script>
      
      function wstawObraz() {
        let image1 = document.createElement("img");
        image1.src = "https://avatars2.githubusercontent.com/u/11339127?s=40&v=4";
        image1.width = 200;
        image1.height = 200;
        document.body.appendChild(image1);
      }

      // dodaj trzy przyciski, ktore zmienia kolor
      // tla strony www:
      // - niebieski
      // - czerwony
      // - (dowolny inny)
      
    </script>
    </body>
</html>
```


## Lekcja 3

Typy danych


```html

<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body id="body">
    <script>
      // TYPY DANYCH
      // TYP LICZBOWY
      let zmienna1 = 123;
      let zmienna2 = -123;
      let zmienna3 = 3.1415;
      
      // zmienna osemkowa
      let zmienna4 = 067;

      // zmienna szesnastkowa
      let zmienna5 = 0xFF;

      // zapis wykladniczy a*10^b
      let zmienna6 = 3.2E5; // 3.2*10^5

      // TYP LANCUCHOWY
      
      let tekst1 = 'Tekst';
      let tekst2= "Tekst2";
      let tekst3 = `${tekst1}`;

      let tekst4 = "\"Krol Lew\"";

      let tekst5 = "test \n test";
      // \n - przejscie do nowej linii
      // \t - tabulator
      // \"
      // \'

      // OPERATORY ARYTMETYCZNE
      // + - * / & Math.pow(a,b)

      let a = 10;
      let b = 20;
      let c = a/b;

      let d = (a+b)*c;

      let e = a&c; 

      let f = Math.pow(2,5);

    
    </script>
    </body>
</html>

```




Jak pobrac dane z formularza?

```html

<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body id="body">
      <form>
        <input id="a" type=text placeholder="Wartosc A">
        <input id="b" type=text placeholder="Wartosc B">
      </form>

      <button onclick=oblicz()>oblicz</button>
    <script>
        function oblicz() {
          let a = document.getElementById("a").value;
          a = parseInt(a);
          let b = document.getElementById("b").value;
          b = parseInt(b);
          window.alert("A + B = "+ (a+b));
        }
    
    </script>
    </body>
</html>

```

### Zadanie

1. Utwórz zmienne (wybierz dla nich intuicyjne nazwy). Następnie przypisz do nich następujące wartości:
   liczbę całkowitą, 1⁄4, 7x10 7 , znak „!”, odpowiedź na pytanie: „czy świeci słońce?”, tekst „C:\TEMP”, „null”.
   Wyświetl na ekranie ich nazwy i wartości.

2. Z pomocą funkcji „prompt()” wczytaj 2 liczby i wykonaj do nich operacje wykorzystując poznane operatory jedno- i
   dwuargumentowe. Korzystając z funkcji „document.write”. Wyświetl te operacje i wyniki na ekranie, np. 3 + 2 = 5,
   3 – 2 = 1, itd.

3. Sprawdź i wyjaśnij wyniki wyrażeń: 2+2*2, (2+2)x2, (3*4)/(20-3x5).

4. Za pomocą 2 kontrolek „input text” wczytaj 2 liczby i oblicz dla nich wyniki
   podstawowych operacji arytmetycznych. Oto przykładowy wynik zapisany w znaczników „div”:
   5 + 2 = 7, 5 - 2 = 3, 5 * 2 = 10, 5 / 2 = 2.5, 5 % 2 = 1.

5. Pobierz od uzytkownika dwie zmienne: a oraz b. Oblicz miejsce zerowe funkcji liniowej `f(x) = ax+b`;

6. Pobierz od uzytkownika dwie zmienne: a, b oraz c. Oblicz i wyświetl miejsca zerowe funkcji `f(x) = ax^2+b*x+c`;



# Instrukcje warunkowe


```html
<html>
    <head>
        <title>Strona w JS</title>
        <!-- czynnosci:
          - budzik
          - prysznic
          - malowanie
          - sniadanie
        -->

        <script>
          function budzik() {
            document.writeln("Dzwoni budzik")
          }

          function prysznic() {
            document.writeln("Biore prysznic")
          }

          function malowanie() {
            document.writeln("Maluje sie")
          }

          function sniadanie() {
            document.writeln("Jem sniadanie")
          }
        </script>
    </head>

    <body>
      <script>
        plec = window.prompt("Podaj płeć");
        wiek = parseInt(window.prompt("Podaj wiek"));
        budzik();
        prysznic();
        // || - lub 1 lub 2 musi zwrocic true
        // && 1 oraz 2 musi zwrocic true

        if (plec == "kobieta" && wiek > 16) {
            malowanie();
        }
        
        // jak budowac wyrazenia?
        // np. 
        // - wiek > 10
        // - wiek < 10
        // - wiek >= 10
        // - wiek <= 10
        // - wiek != 10
        // - wiek % 2

        // 1. Wyświetl na ekranie aktualny dzień tygodnia
        // Jak pobrać aktualny dzień?
        // dzien_tygodnia = new Date().getDay();
        // UWAGA: 0 to niedziela!
        // -------------------------------------
        // 2. Wczytaj liczbę z klawiatury i sprawdz,
        // czy jest parzysta oraz czy jest dodatnia 
        // --------------------------------------
        
        sniadanie();
      </script>
    </body>
</html>
```


## Pętla for

```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body>
      <script>
          var zmienna = -10;
          var przeciwna = -zmienna;
          
          document.write("Badana zmienna: ", zmienna)
          document.write("<br> Wartosc przeciwna: ", przeciwna);
          if(zmienna != 0) {
            var odwrotna = 1 / zmienna;
            document.write("<br> Wartosc odwrotna: ", odwrotna);
          }

          if(zmienna >= 0) {
            document.write("<br> Modul ", zmienna);
          } else {
            document.write("<br> Modul ", przeciwna);
          }

          
          // Rok 2019 jest przestepny ? 
          // 1. rok jest podzielny 4
          //   oraz
          // jest niepodzielny przez 100
          // 2. lub jest podzielna przez 400

          for(var i = 2010;i<=2020;i++) {
            var rok = i;
            var p_1 = rok % 4 == 0 && rok % 100 != 0;
            var p_2 = rok % 400 == 0;

            var przestepny = p_1 || p_2;
            //document.write("<br>---------------------------");
            //document.write(" <br> Rok ", i);
            //document.write(" <br> Czy przestepny ", przestepny);
          }


         //       [0,1,2,3,4,5,6,7,8, ......]
         //       v[4] - piaty element listy
          var v = [110,-10,20,30,0,50,55,-20];

          // liczymy minumum ze zbioru

          // liczba elementów zbioru
          var l = v.length;
          // zalozenie jest takie, ze tablica
          // ma najmniej 1 element

          suma = 0;
          for(var i = 0;i<l;i++) {
            suma = suma + v[i];
          }
          var srednia = suma / l;
          srednia = srednia.toFixed(2);

          var fun = function(a,b) {
              return a-b;
          };

          var sort = v.sort(fun);

          document.write("<br>",sort[0]);



      </script>
    </body>
</html>

```




```html
<html>
    <head>
        <title>Strona w JS</title>
        <style>
          h2 {
            color: white;
            text-align:center;
            background-color: gray;
            font-size:3em;
          }
          .silnia {
            padding:2em;
            display:block;
            width:60%;
            margin:auto auto;
            border:2px solid gray;
            background-color: lightblue;
          }

          .silnia input, .silnia button {
            display:block;
            width:80%;
            margin:auto;
            margin-top:1em;
          }

          .big {
            font-family: monospace;
            font-size: 2em;
            text-align:center;
          }

          .silnia button,.silnia input {
            height: 5em;
            font-size:3em;
          }
        </style>
    </head>
    <body>
      <script>
        
        function oblicz_silnie_do_while(silnia) {
          var wynik = 1;
          var i = 1;
          do {
            wynik = wynik * i;
            i++;
          } while(i <= silnia);
          return wynik;
        }

        function oblicz_silnie_while(silnia) {
          var i = 1;
          var wynik = 1;
          while(i <= silnia) {
            wynik = wynik * i;
            i++;
          }
          return wynik;
        }

        function oblicz_silnie(silnia) {
          var wynik = 1;

          for(var i = 1;i <= silnia;i++) {
           wynik = wynik * i;
          }

          return wynik;
        }          

        function nowe_obliczenie() {
          // pobrac wartosc n od uzytkownika
          let n = document.getElementById("n").value;
          // wykonac obliczenie
          let wynik = oblicz_silnie_do_while(n);
          // wyswietlenie danych
          document.getElementById("wynik").innerHTML = wynik;
        }
      </script>
    

      <div>
        <h2>Kalkulator <var>n!</var></h2>
        <form action="#" class="silnia">
          <input id="n" type="number" min="1" max="20">
          <button onclick="nowe_obliczenie()">Oblicz silnie</button>
        </form>
        <div class="silnia big" id="wynik"></div>
      </div>
  
  </body>
</html>
```

## Inverval


```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body>        
      <p id="akapit"></p>
  
      <script>
        let size = 20;
        
        function dodajTekst() {
          if(size < 1) {
            clearInterval(id);
          }
          let akapit = document.getElementById("akapit");
          akapit.innerHTML += `${size} `;
          size--;
        }

        let id = setInterval(dodajTekst, 200);
      </script>
    </body>
</html>
```


```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body>        
      <div id="blok" style="border:1px solid red;width:300px; height: 300px">
      <p id="timer"></p>
    </div>
  
      <script>
        
        var timer = 300;

        function formatujCzas() {
          //HH:MM:ss
          let h = Math.floor(timer / (60*60));
          let m = Math.floor(timer/60) % 60;
          let s = timer % 60;

          if(m <= 9) {
            m = `0${m}`;
          }
          if(s <= 9) {
            m = `0${s}`;
          }
          return `${h}:${m}:${s}`
        }
        
        function wyswietlCzas() {
          if(timer == 0) {
              clearInterval(tim);
          }
          let el = document.getElementById("timer");
          el.innerHTML = formatujCzas();
          timer-=1;
        }

        let tim = setInterval(wyswietlCzas, 10);

        function getColor() {
          let r = parseInt(Math.random()*255)%255;
          let g = parseInt(Math.random()*255)%255;
          let b = parseInt(Math.random()*255)%255;

          return `rgb(${r},${g},${b})`;
        }

        function nadajKolor() {
          let el = document.getElementById("blok");
          el.style.backgroundColor = getColor();
        }
        
        var max_rozmiar = 300;
        var min_rozmiar = 100;
        var aktualny = 300;

        var dodawanie = false;
       
        function getNewValue() {

          if(dodawanie) {
              aktualny += 1;
          } else {
            aktualny -=1;
          }
          if(aktualny >= max_rozmiar ||  min_rozmiar >= aktualny) {
            dodawanie = !dodawanie;
          }

          return aktualny;
        }
        function zmienRozmiar() {
          let el = document.getElementById("blok");
          let value = getNewValue() + 'px';
          console.log(value);
          el.style.width = value;
          el.style.height = value;
    
        }
        setInterval(zmienRozmiar, 10);
        setInterval(nadajKolor, 1000);
      </script>
    </body>
</html>
```


# Obiekty

```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body>        
      <div id="crocodile"></div>
      <img id="image" width=300></img>
      <script>
        var obrazy = [
          "https://www.sciencedaily.com/images/2018/05/180503101732_1_540x360.jpg",
          "https://assetsnffrgf-a.akamaihd.net/assets/m/102015251/univ/art/102015251_univ_sqr_xl.jpg"
        ];
        
        let krokodyl1 = {
          wiek: 15,
          dlugosc: 2,
          spi: false,
          wyswietl : function() {
            let el = document.getElementById("crocodile");
            el.innerHTML = `Wiek: ${this.wiek} `;
            el.innerHTML += `Dlugosc: ${this.dlugosc} `;
            el.innerHTML += `Spi: ${this.spi} `;
            let img = document.getElementById("image");
            if(this.spi == true) {
              img.src = obrazy[0];
            } else {
              img.src = obrazy[1];
            }
          },
          spij : function() {
            this.spi = true;
            this.wyswietl();
            let x = this;
            setTimeout(function() {
              x.obudz_sie();
            }, 2000);
          },
          obudz_sie : function() {
            this.spi = false;
            this.wyswietl();
          }
        }

        krokodyl1.spij();

      </script>
    </body>
</html>
```


## Zadanie 1

Stwórz obiekt `book`, który będzie posiadał właściwości:
 - title,
 - author,
 - pages,
 - publisher
 
 W obiekcie umieść metodę `print`, która wyświetli w formie listy wszystkie pola.
 
## Zadanie 2
 
 Do tablicy `books` wprowadź 3 ksiązki (z poprzedniego ćwiczenia).
 Przy użyciu pętli for wyświetl wszystkie ksiązki

## Zadanie 3

Utwórz obiekt `zagroda` z właściwościami: 
 - zwierzęta (tablica z gatunkami)
oraz metodą `print`, która po 2 sekundach wyświetli wszystkie zwierzęta w postaci listy `ol`


```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body>        
      <div id="crocodile"></div>
      <img id="image" width=300></img>
      <script>
        var book = {
          title : "Programowanie i tworzenie stron internetowych",
          author: "Agnieszka Klekot",
          pages : 230,
          publisher: "WSiP",
          print : function(root) {
            var lista = document.createElement("ol");
            var title = document.createElement("li");
            title.textContent = this.title;
            lista.append(title);
            var author = document.createElement("li");
            author.textContent =this.author;
            lista.append(author);
            var pages = document.createElement("li");
            pages.textContent = this.pages;
            lista.append(pages);
            var publisher = document.createElement("li");
            publisher.textContent = this.publisher;
            lista.append(publisher);
            root.append(lista);
          }
        }

        var book2 = {
          title : "Programowanie i tworzenie stron internetowych",
          author: "Agnieszka Klekot",
          pages : 230,
          publisher: "WSiP",
          print : function(root) {
            var lista = document.createElement("ol");
            var title = document.createElement("li");
            title.textContent = this.title;
            lista.append(title);
            var author = document.createElement("li");
            author.textContent =this.author;
            lista.append(author);
            var pages = document.createElement("li");
            pages.textContent = this.pages;
            lista.append(pages);
            var publisher = document.createElement("li");
            publisher.textContent = this.publisher;
            lista.append(publisher);
            root.append(lista);
          }
        }

        var book3 = {
          title : "Programowanie i tworzenie stron internetowych",
          author: "Agnieszka Klekot",
          pages : 230,
          publisher: "WSiP",
          print : function(root) {
            var lista = document.createElement("ol");
            var title = document.createElement("li");
            title.textContent = this.title;
            lista.append(title);
            var author = document.createElement("li");
            author.textContent =this.author;
            lista.append(author);
            var pages = document.createElement("li");
            pages.textContent = this.pages;
            lista.append(pages);
            var publisher = document.createElement("li");
            publisher.textContent = this.publisher;
            lista.append(publisher);
            root.append(lista);
          }
        }


        // let root = document.body;

        // var books = [book, book2, book3];

        // for(i = 0;i<3;i++) {
        //   let b = books[i];
        //   b.print(root);
        // }

          var zagroda = {
            zwierzeta : [
              {
                gatunek: "Koza",
                kolor: "Czarny",
                wiek: 3
              },
              {
                gatunek: "Koń",
                kolor: "Biały",
                wiek: 5
              }
            ],
            print: function(root) {
              for(i=0;i<this.zwierzeta.length;i++) {
                let zwierze = this.zwierzeta[i];
                var lista = document.createElement("ul");
                var gatunek = document.createElement("li");
                gatunek.textContent = `${zwierze.gatunek}; wiek ${zwierze.wiek}`;
                lista.append(gatunek);
                root.append(lista);
              }
            }
          }


          zagroda.print(document.body);

      </script>
    </body>
</html>
```



# Konstruktor

```html
<html>
    <head>
        <title>Strona w JS</title>
    </head>
    <body>        
      <div id="crocodile"></div>
      <img id="image" width=300></img>
      <script>

        function Book(title, author, pages, publisher) {
          this.title = title;
          this.author = author;
          this.pages = pages || 0;
          this.publisher = publisher;

          this.print = function(root) {
            var lista = document.createElement("ul");
            var title = document.createElement("li");
            title.textContent = this.title;
            lista.append(title);
            var author = document.createElement("li");
            author.textContent =this.author;
            lista.append(author);
            var pages = document.createElement("li");
            pages.textContent = this.pages;
            lista.append(pages);
            var publisher = document.createElement("li");
            publisher.textContent = this.publisher;
            lista.append(publisher);
            root.append(lista);
          }

        }


        let root = document.body;
        book = new Book(
          "Programowanie i tworzenie stron internetowych",
          "Agnieszka Klekot", 
          200, 
          "WSiP"
        );

        book2 = 

        book3 = new Book(
          "Programowanie i tworzenie stron internetowych",
          "Agnieszka Klekot", 
          200, 
          "WSiP"
        );

        var books = [book, book2, book3];

        for(i = 0;i<3;i++) {
          let b = books[i];
          b.print(root);
        }

      </script>
    </body>
</html>
```


## Zadanie 1
Utworz 3 obiekty typu Car. Każdy samochód powinien mieć ustawione właściwości:
 - name
 - brand
 - engine
 - mile
 - age
 
 oraz metodę `printDetails()`, która wyświetli powyższe właściwości. Wykorzystaj *konstruktor*!
 
 
 ## Zadanie 2
 
 Utwórz konstruktor obiektu Enemy zawierający:
  - name
  - pos_x
  - pox_y
  - speed
  
  oraz funkcję:
    - move(x,y)

Parametry konstruktora: name, speed - reszta domyślnie ustawiona na 0




```html
<html>
    <head>
        <title>Strona w JS</title>
        <style>
          .board {
            width: 600px;
            height: 600px;
            margin: auto auto;
            border:2px solid black;
            background-color: lightgray;
            position: relative;
          }

          .enemy {
            position: absolute;
            width: 100px;
            height: 100px;
            background-image: url(http://icons.iconarchive.com/icons/martin-berube/flat-animal/256/parrot-icon.png);
            background-size: contain;
            background-repeat: no-repeat;
            transition: all 0.5s linear;
          }
        </style>
    </head>
    <body>        
      <div id="board" class="board"></div>
      <button onclick=enemy1.moveLeft()>LEWO</button>
      <button>PRAWO</button>
      <button>GORA</button>
      <button>DOL</button>

      <script>

       function Enemy(name, speed) {
         this.name = name;
         this.speed = speed || 0;
         this.pos_x = 0;
         this.pos_y = 0;

        this.move = function(x,y) {
          this.pos_x = x;
          this.pos_y = y;

          this.enemy.style.left = this.pos_x + "px";
          this.enemy.style.top = this.pos_y + "px";
        }

        this.show = function() {
          let board = document.getElementById("board");
          this.enemy = document.createElement("div");
          this.enemy.classList.add("enemy");
          board.append(this.enemy);
        }
        this.moveLeft = function() {
          this.move(this.pos_x - 5, this.pos_y);
        }
       }

       let enemy1 = new Enemy("Nazwa postaci 1", 10);
       enemy1.show();
       enemy1.move(200,200);

       
      </script>
    </body>
</html>
```


## Formularze

```html
<html>
    <head>
        <title>Strona w JS</title>
        <style>
          #error_msg {
            color: red;
            width: 80%;
            border:1px solid red;
            padding: 0.25em;
            display:none;
          }
        </style>
        <script>
          function zapisz() {
            console.log("TODO!");
            let e = document.forms[0];
            let imie = e.firstName.value.trim();
            if(imie.length == 0) { // imie != ""
              console.log("IMIE JEST PUSTE!");
              let err = document.getElementById("error_msg");
              err.style.display = "block";
            } else {
              let err = document.getElementById("error_msg");
              err.style.display = "none";
              console.log("IMIE JEST UZUPELNIONE!");
              console.log(imie);
            }
          }
        </script>
    </head>
    <body>        
      <form method="POST" action="#" name="welcome">
          <input oninput="zapisz()" type="text" name="firstName" />
          <p id="error_msg">Podałeś błędne dane</p>
          <!-- <input type=button value="Wyslij dane" onclick="zapisz()"> -->
      </form>
    </body>
</html>
```



```html
<html>
    <head>
        <title>Strona w JS</title>
        <style>
          #error_msg {
            color: red;
            width: 80%;
            border:1px solid red;
            padding: 0.25em;
            display:none;
          }
        </style>
        <script>
          function zapisz() {
            console.log("TODO!");
            let imie = document.forms[0].firstName.value;
            console.log(imie);
            var ileCyfr = imie.match(/[0-9]/g);
            // var ileLiter = imie.match(/[a-zA-Z]/g);
            // var ileBialychZNakow = imie.match(/ /g);
            // var ileZnakowSpecjalnych = imie.match(/[!@#$%^&*]/g);
            
            if(ileCyfr == undefined) {
              ileCyfr = 0;
            } else {
              ileCyfr = ileCyfr.length;
            }
            // 8 cyfr, 2 znaki specjalne oraz 2 male litery
            console.log(ileCyfr);

          }
        </script>
    </head>
    <body>        
      <form method="POST" action="#" name="welcome">
          <input oninput="zapisz()" type="text" name="firstName" />
          <p id="error_msg">Podałeś błędne dane</p>
          <!-- <input type=button value="Wyslij dane" onclick="zapisz()"> -->
      </form>
    </body>
</html>
```



## AJAX

```html
<html>
    <head>
        <title>Strona w JS</title>
        <style>
        </style>
        <script>

        function pobierzDane() {
          var xmlhttp = new XMLHttpRequest();
          var url = "https://jsonplaceholder.typicode.com/users";

          xmlhttp.onreadystatechange = function() {
          if (this.readyState == 4 && this.status == 200) {
            var a = JSON.parse(this.responseText);
            console.log(a);
            wyswietl(a);
          }
        };
        xmlhttp.open("GET", url, true);
        xmlhttp.send();
        }
        function wyswietl(e) {
          for(var i = 0;i<e.length;i++) {
            console.log("UZYTKOWNIK"+i);
            let user = e[i];
            console.log(user);
          }

        }
        </script>
    </head>
    <body>        
      <button onclick=pobierzDane()>Pobierz</button>
    </body>
</html>
```
